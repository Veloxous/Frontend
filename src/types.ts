// Screens in the Veloxous click-through. 'how' and 'learn' currently route to
// Explore (the public, gate-free surfaces) — mirrors the source prototype.
export type Screen =
  'landing' | 'connect' | 'explore' | 'how' | 'learn' | 'deposit' | 'portfolio' | 'withdraw'
